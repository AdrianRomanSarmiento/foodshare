import { PlatosFilterPipe } from './platos-filter.pipe';


describe('PlatosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
